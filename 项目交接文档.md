# Wallhaven Downloader 项目交接文档

> 本文档用于向新的 AI 助手 / 开发者交接项目全貌。请先读完本文档，再阅读具体源码。
> **最后更新：2026-09-14**（已包含 pixiv 数据源、图片直连架构、设置页改版等重大变更）
> 代码仓库：https://github.com/1474846859/wallhaven-downloader.git

---

## 一、项目概述

**项目名称**：Wallhaven Downloader（多站壁纸 / 插画下载器）
**类型**：Electron 桌面应用（Windows 优先）
**核心定位**：**三数据源**的插画浏览与批量下载工具

| 数据源 | 数据获取方式 | 说明 |
|---|---|---|
| **Wallhaven** | 官方 JSON API | `https://wallhaven.cc/api/v1`（dev 模式经 vite 代理转发） |
| **yande.re** | 网页 HTML 抓取 | 解析页面内嵌的 `Post.register({...})` |
| **pixiv** | **官方网页 JSON 接口** | 不再使用 HTML 解析（详见第六节） |

**主要能力**：
- 三站各自浏览 / 搜索（各自的榜单、筛选、日历、搜索框）
- 勾选 → 批量下载（实时进度、history 去重、失败重试、可取消）
- **三站各自独立的下载目录**
- pixiv 专属：账号登录、R-18 / AI 榜单、作品详情页（多页浏览）、生成 IDM 直链
- 导出下载直链，交给 IDM 批量下载
- 全屏查看原图（可选画质 + 相邻页预加载）

**重要前提**：三个图源在国内**都需要挂代理才能访问**（Wallhaven / yande.re / pixiv）。
代理开关与端口在「设置 → 系统设置」里统一配置，作用于全软件。

---

## 二、技术栈与环境

| 类别 | 技术 |
|---|---|
| 运行时 | Electron 27.x |
| 构建 | electron-vite 2.x + Vite 5.x |
| UI | React 18（函数组件 + Hooks；**无状态管理库、无路由**） |
| 打包 | electron-builder 24.x（Windows NSIS） |
| 网络 | axios 1.6、https-proxy-agent 9.x |
| 并发控制 | p-limit 5.x |
| 更新 | electron-updater 6.x（**已接入 GitHub Releases**，见第六节 6.8） |

**运行环境要求**：
- Node.js（实测 v18.17.1 / v24 均可）
- ⚠️ 本机**不得存在环境变量 `ELECTRON_RUN_AS_NODE`**（详见第十二节）
- ⚠️ 需要本机代理（VPN / 加速器）可用，端口与「系统设置」一致

---

## 三、目录结构

```
wallhaven-downloader/
├── package.json                    # 依赖、脚本、electron-builder 配置
├── electron.vite.config.js         # 三进程构建配置（dev 代理端口从用户配置读取）
├── scripts/release.cjs             # ★发布脚本：读 .env 的 GH_TOKEN → 调 electron-builder 上传
├── 项目交接文档.md                  # 本文档
├── release/                        # 打包产物（安装包 + win-unpacked）
└── src/
    ├── main/                       # 【主进程】Node 环境
    │   ├── index.js                # 入口：窗口 / 会话网络 / IPC / 三站抓取（★核心，约 900 行）
    │   ├── store.js                # 配置与历史持久化（含写盘防抖）
    │   ├── cache.js                # 通用 TTL+LRU 缓存（作品页地址 / 作品详情），可整体清空
    │   ├── proxyHelper.js          # 代理 agent 生成（带缓存复用 + keep-alive）
    │   ├── updater.js              # ★自动更新（electron-updater + GitHub Releases）
    │   ├── pixivApi.js             # ★pixiv 官方接口封装（榜单/发现/关注/详情/页地址/批量直链）
    │   ├── downloadManager.js      # Wallhaven 下载管理器（p-limit 并发）
    │   ├── yandereDownloadManager.js # yande.re 下载管理器（并发 + 可取消 + 重试）
    │   └── pixivDownloadManager.js # pixiv 下载管理器（API 取真实原图 / 多页 / 指定页下载）
    ├── preload/
    │   └── index.js                # contextBridge 暴露 electronAPI
    ├── renderer/                   # 【渲染进程】浏览器环境
    │   ├── index.html
    │   └── src/
    │       ├── main.jsx            # React 挂载入口
    │       ├── App.jsx             # ★唯一状态中枢（约 1000 行）
    │       ├── App.css             # 全部样式（约 1200 行）
    │       ├── styles/index.css
    │       └── components/
    │           ├── TypeTabs.jsx        # ★顶部第二行"分区/浏览类型"标签（pixiv 标签唯一数据源）
    │           ├── Sidebar.jsx         # Wallhaven 左栏（搜索 / 时间范围 / 筛选 / 目录）
    │           ├── YandeRePanel.jsx    # yande.re 左栏（页数 / 日历 / IDM 链接）
    │           ├── PixivPanel.jsx      # pixiv 左栏（搜索 / 日期 / 页数 / IDM 链接）
    │           ├── PixivIllustDetail.jsx # ★pixiv 作品详情页（多页 / 全屏原图 / 直链 / 选页下载）
    │           ├── ImageGrid.jsx       # 图片网格（瀑布流 / 骨架屏 / 多页角标 / AI 标 / 无限滚动）
    │           ├── ImagePreview.jsx    # 预览浮层（Wallhaven、yande.re 使用）
    │           ├── DownloadPanel.jsx   # 右栏下载面板
    │           ├── SettingsPanel.jsx   # 全屏设置页（★4 个分区标签）
    │           ├── Calendar.jsx        # 日历（yande.re 与 pixiv 共用）
    │           ├── Toast.jsx           # ★应用内提示（替代 alert）
    │           ├── CategoryBar.jsx     # 【未使用】
    │           ├── SearchBar.jsx       # 【未使用】⚠️ 导出组件名其实是 SettingsBar（命名颠倒）
    │           └── SettingsBar.jsx     # 【未使用】⚠️ 导出组件名其实是 SearchBar（命名颠倒）
    └── resources/                  # 【空目录】预留（打包图标引用的是「项目根目录」的 resources/，见下方说明）
```

⚠️ `package.json` 的 `build.win.icon` 引用的是 **`resources/icon.ico`（相对项目根目录）**，
但**根目录下没有 `resources/` 目录**（只有这个空的 `src/resources/`）。
实测 `npm run dist` **不会因此失败**（electron-builder 会回退成 Electron 默认图标），
但如果想要自己的图标，需要创建根目录 `resources/` 并放入 `icon.ico`。

---

## 四、架构分层与文件职责

### 4.1 主进程（src/main/）

**index.js（核心，约 900 行）**
- **GPU 开关**：`shouldEnableHardwareAcceleration()` 在 app ready **之前**同步读 `config.json`；未开启则 `app.disableHardwareAcceleration()`（默认关闭 = 软件渲染，稳定优先）
- **窗口**：隐藏菜单栏；1200×800；`contextIsolation:true` + `nodeIntegration:false`；dev 加载 `localhost:5173` 并开 DevTools
- **会话网络（`setupPixivImageNetworking`）**：⚠️ 名字是历史遗留，实际负责三件事：
  1. `session.setProxy({ proxyRules })` —— **三站共用**的全会话代理（本地地址 Chromium 默认绕过，不影响 dev 服务器）
  2. `webRequest.onBeforeSendHeaders` 给 `*.pximg.net` **注入 Referer**（+ 登录 Cookie），绕过防盗链
  3. 代理开关/端口变化时重新应用（由 `save-app-config`、`save-pixiv-config` 触发）
- **`request(url, options)`**：Wallhaven 用的统一 GET（按 config 决定是否走 HttpsProxyAgent）
- **取消标记**：模块级 `downloadCancelFlag` + `isDownloadCancelled()`
- **下载进度节流**：`createProgressSender()` —— `progress` 事件按 id 合并、每 150ms 批量推送；`item` / `complete` 立即发送
- **三站抓取 + 全部 IPC 处理器**（见第七节）

**store.js**
- 惰性 `app.getPath('userData')`；持久化 `history.json` 与 `config.json`
- `loadConfig()` **深度合并**（老配置不丢、新字段自动补默认）
- `saveHistory()` **写盘防抖（最多 800ms 一次）**；`flushHistory()` 在 `app.on('before-quit')` 强制落盘

**cache.js**
- 通用 `TTLCache(maxSize, ttl)`：Map + TTL + **LRU 淘汰** + 命中统计 + `peek()`
- 导出两个实例：`pagesCache`（作品每页原图地址）、`illustDetailCache`（作品详情），各 800 条 / 30 分钟
- `clearAllCaches()`：在「清空下载缓存」「清理并删除」「pixiv 重新登录或 cookie 变化」时调用（避免命中旧账号数据）

**proxyHelper.js**
- `createProxyAgent(useProxy, proxyPort)`：**按端口缓存复用**同一个 `HttpsProxyAgent`（keep-alive）
- ⚠️ 关键：若每次新建 agent，会导致每张图都重新 TCP+TLS 握手（走 VPN 时明显变慢）

**pixivApi.js**（pixiv 全部接口，详见第六节）
- `pixivGet(url, options)`：统一带 Cookie / Referer / 代理的 GET
  - ⚠️ 不用代理时**必须显式传 `proxy: false`**，否则 axios 会去读系统环境变量里的 `HTTP_PROXY`，导致行为不一致
  - 默认：`timeout 30s`、`Referer: https://www.pixiv.net/`、Chrome UA、`Accept-Language: zh-CN`
- `getIllustPages` / `getIllustDetail`（均带缓存）、`getRanking`、`getDiscovery`、`getFollowLatest`、`collectIllustUrls`
- `getPixivSettings()`：**校验 baseUrl**（必须是以 `pixiv.net` 结尾的 http(s)，否则回退默认值并警告）

**三个下载管理器**
| 文件 | 并发 | 每张间隔 | 特点 |
|---|---|---|---|
| `downloadManager.js` | `Math.min(n, 16, Math.max(8, n))`（即 8~16 区间） | — | 先取详情再流式下载 |
| `yandereDownloadManager.js` | 1~10（默认 6） | 800ms | 直链下载、可取消、重试、429 等 15s |
| `pixivDownloadManager.js` | 1~12（默认 6） | 300ms | **先调 /pages 拿真实原图**、支持多页、可取消；另有 `pixivDownloadUrls` 供"指定页"下载 |

### 4.2 预加载（src/preload/index.js）
- `contextBridge.exposeInMainWorld('electronAPI', {...})` 暴露约 20 个方法（见第七节）
- 渲染层只能通过 `window.electronAPI.*` 访问主进程能力

### 4.3 渲染进程（src/renderer/src/）
- **`App.jsx` 是唯一状态中枢**（约 1000 行），所有子组件均为**受控组件**
- 无路由、无状态管理库；用 `useMemo` 做列表过滤，用 `useCallback` + `React.memo` 降低重渲染
- **`TypeTabs.jsx` 是 pixiv 浏览类型的唯一数据源**（顶部标签渲染 + 设置页勾选都引用它）
- `Toast.jsx` 提供全局 `toast()`，替代 `alert`（不阻塞渲染、可一键复制内容）

---

## 五、核心功能清单

### 5.0 主界面布局（已改版）
```
┌──────────────────────────────────────────────────────┐
│ 第一行：[Wallhaven] [yande.re] [pixiv]         ⚙️设置 │
├──────────────────────────────────────────────────────┤ ← 分割线
│ 第二行：当前来源的"分区 / 浏览类型"标签（可自定义显隐，会自动换行）│
├──────────────────────────────────────────────────────┤
│ 左栏（各站不同）  │      图片网格       │   下载面板    │
└──────────────────────────────────────────────────────┘
```
- **分区 / 浏览类型按钮已从左侧栏迁移到顶部第二行**（`TypeTabs.jsx`）
- 左侧栏因此腾出空间，只保留该站的其它功能（搜索、筛选、日历、IDM 链接等）
- 点击图片的行为：
  - **pixiv** → 打开**作品详情页**（`PixivIllustDetail`）
  - Wallhaven / yande.re → 打开**预览浮层**（`ImagePreview`）

### 5.1 三站通用
| 功能 | 说明 |
|---|---|
| 数据源切换 | 顶栏三个按钮；切换时清空列表 / 选中 / 下载任务 / 预览 |
| 默认图片来源 | 系统设置里可选，写入 `defaultSource`，下次启动生效 |
| 图片网格 | 瀑布流自适应；骨架屏；勾选框；宽高比 >1.5 的宽图跨 2 列；**滚到底自动加载下一页**（IntersectionObserver，提前 400px） |
| 批量下载 | 右栏「批量下载」；实时进度；history 去重（已存在→`skipped`）；失败可「重新下载」；可「取消下载」；可「清空下载列表」 |
| 三站独立下载目录 | Wallhaven 用顶层 `downloadDir`；yande.re 用 `yandere.downloadDir`；pixiv 用 `pixiv.downloadDir` |
| 全屏预览 | 点卡片或 🔍 打开；`Esc` 关闭、`←/→` 切换 |

### 5.2 Wallhaven
- **搜索**：关键词搜索（`sorting=relevance`）
- **7 种分区**（顶部标签）：全部 / 最新 / 近期热门 / 顶级列表 / 随机 / 最多浏览 / 最多收藏
- **排行榜时间范围**（左栏，仅"顶级列表"可用）：1天 / 3天 / 1周 / 1月 / 3月 / 6月 / 1年
- **筛选**（左栏）：类别（常规/动漫/真人）、等级（SFW/Sketchy/NSFW）、分辨率（`atleast`）、比例（`ratios`）
- **API Key**：设置页可填
- 搜索请求参数：
  ```
  GET {API_BASE}/search?q=&page=&sorting=&order=&purity=&categories=[&topRange=][&atleast=][&ratios=][&apikey=]
  ```
  - dev：`/api/v1`（由 vite server.proxy 转发，**端口从用户 config.json 读取**）
  - 生产：`https://wallhaven.cc/api/v1`（渲染层直连）

### 5.3 yande.re
- **5 种浏览类型**（顶部标签）：前24小时 / 按日 / 按周 / 按月 / 最新
- **日历**（左栏；按日/按周/按月时显示，按周会自动换算到该周周一）
- **页数选择 + 上一页/下一页**（左栏，仅"最新"时显示）
- **IDM 批量链接**（左栏）：生成直链 / 暂存（跨页累计去重）/ 全选复制 / 清空

### 5.4 pixiv（本项目建设的重点）
| 功能 | 说明 |
|---|---|
| **账号登录** | 设置页「打开 pixiv 登录窗口」→ 内嵌真实登录页（可过验证码）→ 登录成功后自动抓取 cookie 并保存 |
| **浏览类型**（顶部标签） | 发现 / 我的关注 / 日榜 / 周榜 / 月榜 / 新人榜 / 原创榜 / 日榜R18 / 周榜R18 / 月榜R18 / **AI日榜** / **AI日榜R18** |
| **标签自定义** | 设置页勾选哪些标签显示在顶部（持久保存；按预设顺序排列、放不下自动换行） |
| **历史榜单** | 选日期即可查看当日榜单（日/周/月及 R-18 榜均支持） |
| **搜索** | 左栏搜索框（走 `/ajax/search/artworks`） |
| **作品详情页** | 点卡片进入：左上返回、网格展示**全部页**（默认中等图）、底部标签 / 作者 /「打开作者主页」、全屏看原图（可选画质）、⚡生成直链 / 📋复制直链 / ⬇加入下载队列（可勾选部分页） |
| **多页角标** | 卡片右上角显示 `Np`（仅多页作品） |
| **AI 角标** | 卡片左下角显示 `AI`（发现 / 关注 / 搜索 / AI 榜有效；**普通榜单接口不返回 aiType，无法判断**） |
| **显示过滤** | 设置页两个开关：显示 R-18 / 显示 AI（客户端过滤；pixiv 服务端也会按账号设置过滤一次） |
| **预加载** | 设置页可调"向后 / 向前"预加载张数（默认 4 / 1） |
| **浏览画质** | 设置页可选：自动（中等图 → 无缝换原图）/ 仅中等图 / 仅原图 |

---

## 六、关键实现机制（重点，改代码前必读）

### 6.1 pixiv 数据获取：走官方 JSON 接口，不做 HTML 解析
| 用途 | 接口 | 关键参数 |
|---|---|---|
| 发现（推荐流） | `/ajax/illust/discovery` | `mode=all`、`p=N`（主进程**一次合并 3 页**，避免首屏太空） |
| 关注流（需登录） | `/ajax/follow_latest/illust` | `p=N`、`mode=all`、`lang=zh`（只返回 id 列表，再用详情接口并发 8 补齐，默认取前 24 个） |
| 榜单 | `/ranking.php` | `mode=` 见下方白名单、`format=json`、`date=YYYYMMDD`、`p=N` |
| 搜索 | `/ajax/search/artworks/{关键词}` | `word=`、`p=`、`order=date_d`、`mode=all`、`s_mode=s_tag`、`type=all` |
| 作品详情 | `/ajax/illust/{id}` | — |
| 作品各页地址 | `/ajax/illust/{id}/pages` | — |

**`RANKING_MODES` 白名单**（`src/main/index.js`，共 14 项；**不在白名单里的 mode 会被静默替换成日榜**）：
```
daily, weekly, monthly, rookie, original, male, female,
daily_r18, weekly_r18, monthly_r18, male_r18, female_r18,
daily_ai, daily_r18_ai
```
> ⚠️ 前端 `TypeTabs.jsx` 的 `PIXIV_TABS` 是浏览类型的**【唯一数据源】**（被顶部标签、设置页勾选、后端白名单三处引用）。
> **新增 / 删除榜单必须同步改 `RANKING_MODES`**，否则标签会静默显示成"日榜"（新增 `daily_ai` 时踩过此坑）。
> ⚠️ **AI 榜只有日榜**（`weekly_ai` / `monthly_ai` 实测 404，不存在）。

**要点**：
- 所有请求都带 `Referer: https://www.pixiv.net/` + Cookie，并用 `HttpsProxyAgent`
- **`date` 只对日 / 周 / 月榜有效**：带日期请求 R-18 榜时服务端会**忽略 date 返回今日榜单**（代码里已对"R-18 + 日期"做了兼容处理）
- 发现 / 关注 / 搜索接口都会返回 `illustType` / `aiType` / `pageCount`，用于角标与过滤
- 关键词需 `encodeURIComponent`
- **`fetch-pixiv-posts` 的 `tags` 是自研字符串协议**：`ranking:模式:日期`（日期可省）/ `search:关键词` / `discovery:` / `follow:`
- 搜索 URL 形如：`{baseUrl}/ajax/search/artworks/{kw}?word={kw}&order=date_d&mode=all&p=N&s_mode=s_tag&type=all`
- 搜索返回体兼容两种结构：`body.illustManga.data`（新版，插画+漫画合并）或 `body.illust.data`（旧版）

### 6.2 图片加载：直接访问 i.pximg.net（不用自定义协议）
**旧方案（已删除）**：`pixiv-img://` 自定义协议，由主进程下载再回传 —— 无法利用 HTTP/2 与磁盘缓存，多页详情页明显卡顿。
**现方案**：渲染层 `<img src="https://i.pximg.net/...">` **直连**，靠 Chromium 网络栈（HTTP/2 多路复用 + 磁盘缓存 + 并行解码）：
1. `session.setProxy()` 让整个会话走用户代理
2. `webRequest.onBeforeSendHeaders` 给 `*.pximg.net` **注入 `Referer: https://www.pixiv.net/`**（+ 已登录时的 Cookie），破解防盗链
3. `pixivApi.js` 中 `referer: 'https://www.pixiv.net/'` 附近保留了**历史说明注释**，勿随意删除

> ⚠️ 若以后改回自定义协议，图片加载性能会明显回退，**不建议**。

### 6.3 代理体系（三站共用）
- **会话级**：`session.setProxy({ proxyRules: 'http=127.0.0.1:PORT;https=127.0.0.1:PORT' })` → 影响所有网页请求（含图片、登录窗口）
- **请求级**：主进程 axios 请求使用 `createProxyAgent()`（**按端口缓存复用**，keep-alive）
- 关闭代理时用 `ses.setProxy({ mode: 'direct' })`
- 开关 / 端口变更时两处都要更新：`save-app-config`、`save-pixiv-config` 都会调用 `setupPixivImageNetworking()`
- ⚠️ **历史坑**：曾用 PAC 按域名分流（只让 pixiv 走代理），结果 Wallhaven / yande.re 的图片直连超时 —— 已废弃，现在**三站统一走代理**
- pixiv 登录窗口使用**独立 session**，需单独 `setProxy`

### 6.4 pixiv 登录（cookie 抓取）
- 打开独立窗口加载 `https://www.pixiv.net/`，轮询检测是否已登录
- 登录成功后抓取该 session 的 cookie（关键项 `PHPSESSID`），保存进配置
- 保存后必须调用 `clearAllCaches()`（否则会命中旧账号的缓存数据）
- 登录窗口关闭时**解绑** `onBeforeSendHeaders` 监听（避免监听器泄漏）

### 6.5 pixiv 图片 URL 规则 / 画质档位
| 档位 | URL 变换 |
|---|---|
| **原图** | `/img/.../xxx_p0.jpg` → `/img-original/.../xxx_p0.jpg`（WebP / JPEG 后缀需逐个尝试） |
| **中等图** | `/c/540x540_70/img-master/...`（接口直接返回，秒开） |
| **小图** | `/c/250x250_80_a2/...` |
- 多页作品：`p0`、`p1`… 分别对应 `/ajax/illust/{id}/pages` 返回的每一项
- ⚠️ 榜单接口返回的 `url` 形如 `https://i.pximg.net/c/240x480/img-master/...`，**是缩略图**，不能直接当原图用
- `collectIllustUrls()`（批量直链）会按需调 `/pages` 拿真实原图地址

### 6.6 全屏大图：双层叠加 + 淡入（避免黑闪）
```
上层：原图    ← 加载完成后 opacity 0 → 1（0.18s 淡入），元素不重建
底层：中等图  ← 一直显示，垫在下面
```
- ⚠️ **不要**给 `<img>` 加形如 `${index}_${src}` 的 key：src 变化时 React 会卸载并重建元素，中间那一帧空白就是**黑闪**
- 翻页时只重建底层（避免残留上一张），同页内切换画质不重建
- 设置里选「仅中等图」/「仅原图」时走单层逻辑

### 6.7 无限滚动
- `ImageGrid.jsx` 中最后一个卡片挂 `ref`，`IntersectionObserver`（`rootMargin: '400px'`）触发 `onLoadMore`
- `isLoading` / `hasMore` 双闸门防止重复请求

### 6.8 自动更新（electron-updater + GitHub Releases）

**发布方（你）的流程**：
```bash
# 1) 改 package.json 的 version（如 1.3.1 → 1.3.2）  ★ 必须！否则检测不到更新
# 2) 项目根建 .env（.gitignore 已忽略），写入： GH_TOKEN=你的GitHubToken（repo 权限）
# 3) 一条命令打包 + 发布
npm run release          # 实际执行 scripts/release.cjs
```

**⚠️ 为什么 release 要套一层 `scripts/release.cjs`**（两个已踩的坑）：
1. **electron-builder 不会读项目根的 `.env`** —— 找不到 `GH_TOKEN` 时，`--publish always`
   会**静默跳过上传**（不报错、退出码 0、GitHub 上什么都没有）。包装脚本负责把 `.env` 里的
   token 注入环境变量后再调用它。
2. **`releaseType` 默认是 `draft`** —— 草稿对用户端**不可见**（electron-updater 只认已发布的
   Release，`GET /releases` 不带 token 也看不到草稿）。已在 `build.publish` 显式写
   `"releaseType": "release"`；若第一次仍成了草稿，去 Releases 页面点 **Publish release** 即可。
它会自动：打包 → 创建 GitHub Release（tag `v1.3.1`）→ 上传 3 个文件：
`Wallhaven-Downloader-Setup-1.3.1.exe`、`....exe.blockmap`、`latest.yml`

**客户端**：启动 8 秒后静默检查一次；「设置 → 系统设置 → 软件更新」可手动检查 / 下载（带进度条）/ 重启安装。
有新版时 App 会弹 Toast 提示。开发模式（未打包）下自动跳过并提示。

**⚠️ 三条铁律**（违反任一条都会导致"检测不到更新"）：
1. **发版前必须改 `version`** —— 判断只看 `package.json` 的 version，与安装包文件名无关
   （历史上一直没改版本号、只手动重命名文件，所以内部版本永远是 1.0.0）
2. **Release 里必须有 `latest.yml`** —— 用 `npm run release` 自动带上；手动上传极易漏传，
   或出现 yml 里写连字符名、实际文件是空格名 → 下载 404（本项目踩过）
3. **`electron-updater` 必须保持 external**（见 `electron.vite.config.js`）—— 若被打包进 bundle，
   它内部的 `require('electron')` 会被 vite 的 SSR 转换改写，`ElectronAppAdapter` 取不到 `app`，
   **启动即崩**：`TypeError: Cannot read properties of undefined (reading 'getVersion')`

**代理（实测关键点）**：electron-updater 使用独立 session
（`session.fromPartition('electron-updater', { cache: false })`），**不继承** `defaultSession` 的代理。
`setupPixivImageNetworking()` 与 `updater.js` 各同步一次。

实测结论（2026-09-20）：`api.github.com` 国内**可能直连可通**，但
`github.com/.../releases/download/...`（资产/清单下载）**直连超时、必须走代理** ——
也就是说**不给这个 session 设代理，检查更新会失败**。手工验证方式：
```bash
curl -L -x http://127.0.0.1:<代理端口> \
  https://github.com/<owner>/<repo>/releases/latest/download/latest.yml
# 应返回 version / files / path / sha512 等字段（这就是 electron-updater 读的清单）
```

**GitHub Token 申请**：GitHub → Settings → Developer settings → Personal access tokens →
Generate new token (classic) → 勾 `repo` → 复制（只显示一次）→ 写进 `.env`。
Token 只存在你本机，**绝不能打进软件**。

---

## 七、IPC 接口清单

### 7.1 渲染 → 主进程（`ipcRenderer.invoke`，共 23 个）
| 频道 | 参数 | 说明 |
|---|---|---|
| `get-config` | — | 读全量配置（`global.config`） |
| `save-config` | `config` | Wallhaven 侧配置（apiKey / concurrency / downloadDir / purityFlags / topRange / useProxy / proxyPort）**合并**保存 |
| `save-app-config` | `patch` | 全局配置（defaultSource / useProxy / proxyPort / hardwareAcceleration）；**代理的唯一写入口**：合并 → 同步三站子配置 → 重设会话代理 → 落盘 |
| `get-yandere-config` / `save-yandere-config` | 配置对象 | yande.re 独立配置（**忽略**代理字段，防止写回旧值） |
| `get-pixiv-config` / `save-pixiv-config` | 配置对象 | pixiv 独立配置；cookie 变化时会 `clearAllCaches()` 并重设会话代理 |
| `get-history` | — | 读下载历史 |
| `select-download-dir` | — | 打开目录选择框 |
| `download-images` | `(imageIds, downloadDir, concurrency, source, imageData)` | **三站统一的批量下载入口** |
| `cancel-download` | — | 置取消标记（当前任务结束后停止） |
| `clear-download-cache` | — | 清空下载记录（history） |
| `delete-all-downloads` | — | 删除三站下载目录内文件 + 清记录（**危险操作，UI 已二次确认**） |
| `fetch-image-detail` | `imageId` | Wallhaven 单图详情（自动带 API Key） |
| `fetch-yandere-posts` | `(tags, page, limit)` | yande.re 列表（HTML 抓取） |
| `fetch-pixiv-posts` | `(tags, page, limit)` | pixiv 列表（`tags` 协议见 6.1） |
| `fetch-pixiv-pages` | `illustId` | 该作品**每一页**的真实原图地址 |
| `fetch-pixiv-detail` | `illustId` | 作品详情（分级 / 标签 / 作者 / 原图） |
| `pixiv-follow-user` | `(userId, follow)` | 关注 / 取关画师（需登录） |
| `pixiv-collect-links` | `ids` | 批量收集原图直链（并发 8，带进度事件） |
| `pixiv-download-pages` | `(urls, downloadDir)` | 下载指定 URL 列表（详情页"选中的页"） |
| `pixiv-login` | — | 打开内嵌登录窗口，登录成功后回传配置 |
| `open-external` | `url` | 用系统默认浏览器打开（如 pixiv 作者主页） |
| `get-app-version` | — | 当前软件版本（`app.getVersion()`，即 package.json 的 version） |
| `get-update-status` | — | 最近一次更新状态（设置页打开时主动拉一次，避免错过启动时的静默检查结果） |
| `check-update` | — | 检查更新（读 GitHub Release 的 latest.yml 比对版本） |
| `download-update` | — | 下载更新包（支持差分，通常只有几百 KB） |
| `install-update` | — | 退出并安装（`quitAndInstall(false, true)`：弹安装向导 + 装完自动启动） |

### 7.2 主进程 → 渲染（事件）
| 频道 | 说明 |
|---|---|
| `download-progress` | 下载进度。`data.type` 三种：`item`（单任务状态，立即推送）/ `progress`（批量，**每 150ms 合并推送**）/ `complete`（全部完成） |
| `pixiv-links-progress` | 批量收集直链的进度 `{done, total}` |
| `update-status` | 更新状态：`checking` / `available` / `not-available` / `downloading`（含 percent）/ `downloaded` / `error`。⚠️ 监听请用 `onUpdateStatus()` **返回的取消函数**，不要用 `removeAllListeners`（App 与设置页同时监听同一频道，整体清空会误伤另一方） |

> 渲染层封装：`onDownloadProgress()` / `removeDownloadProgressListener()`、`onPixivLinksProgress()` / `removePixivLinksProgressListener()`
> ⚠️ 组件卸载时**务必**调用对应的 remove 监听（`removeAllListeners`），否则会叠加多个回调

### 7.3 图片数据形状（前端归一化结构）
`App.jsx` 的 `images` 数组元素（三站统一）：
```js
{
  id, url, thumbnail, width, height,
  pageCount,   // 多页作品总页数（>1 才显示 Np 角标）
  aiType,      // pixiv：1=AI 生成（0/2=非 AI）；其它站为 undefined
  xRestrict,   // pixiv：1=R18
  tags: []
}
```
- ⚠️ 网格渲染统一读 `thumbnail` + `pageCount` + `aiType`，新增来源时**必须**按此结构返回

---

## 八、性能相关的关键实现（请勿回退）

| 优化 | 位置 | 原因 |
|---|---|---|
| **图片直连 i.pximg.net** | 渲染层 `<img>` + 主进程注入 Referer | 走 HTTP/2 + 磁盘缓存，比自定义协议转发快得多 |
| **代理 agent 复用** | `proxyHelper.js` | 避免每张图重做 TCP + TLS 握手（走 VPN 时尤其明显） |
| **下载进度 150ms 节流** | `createProgressSender()` | 否则 IPC 消息洪水会拖垮渲染 |
| **history 写盘防抖** | `store.js saveHistory()` | 减少磁盘 IO（最多 800ms 一次） |
| **TTL + LRU 缓存** | `cache.js` | 作品页地址 / 详情高频复用，30 分钟有效 |
| **列表过滤 `useMemo`** | `App.jsx` | 避免每次渲染重算（R18 / AI 过滤） |
| **`React.memo` + `useCallback`** | 各组件 | 减少无关重渲染 |
| **`content-visibility: auto`** | `App.css` 卡片 | 长列表中离屏卡片跳过布局与绘制 |
| **懒加载 `loading="lazy"`** | 网格图片 | 滚动到附近才请求 |
| **无限滚动提前 400px** | `ImageGrid.jsx` | 滚动到底无需等待 |
| **GPU 加速开关（默认关）** | `index.js` | 默认软件渲染更稳；用户可在系统设置里打开 |

---

## 九、配置文件结构（`%APPDATA%/wallhaven-downloader/config.json`）

```jsonc
{
  // ===== 全局（wallhaven 主配置 + 跨站设置，见 src/main/store.js 的 defaultConfig） =====
  "apiKey": "",
  "concurrency": 16,
  "downloadDir": null,
  "purity": "100",                  // 旧字段，仅作兼容保留
  "purityFlags": { "sfw": true, "sketchy": false, "nsfw": false },
  "topRange": "1M",
  "useProxy": true,                 // 全局代理开关（作用于三站）
  "proxyPort": 12450,
  "defaultSource": "wallhaven",     // wallhaven | yandere | pixiv
  "hardwareAcceleration": false,

  "yandere": {
    "baseUrl": "https://yande.re",
    "concurrency": 6,
    "limit": 100,
    "downloadDir": null,
    "useProxy": true,               // 由「系统设置」统一同步，不要单独改
    "proxyPort": 7890               // 运行时会被全局值覆盖（当前为 12450）
  },

  "pixiv": {
    "baseUrl": "https://www.pixiv.net",
    "cookie": "",                   // 登录后自动写入（关键项 PHPSESSID）
    "downloadDir": null,
    "concurrency": 6,
    "limit": 100,
    "rankingMode": "daily",         // 榜单默认模式（实际列表由前端标签决定）
    "useProxy": true,
    "proxyPort": 7890,
    "showR18": true,                // 客户端过滤 R-18（默认显示）
    "showAI": true,                 // 客户端过滤 AI
    "visibleTabs": ["daily", "weekly", "monthly"],   // 顶部显示哪些标签
    "preloadForward": 4,
    "preloadBackward": 1,
    "previewQuality": "auto"        // auto | regular | original
  }
}
```
> ⚠️ `yandere` 的浏览类型（`hotType`）、`pixiv` 的榜单日期（`rankingDate`）**不在配置里持久化** ——
> 它们是 `App.jsx` 的 React state，每次启动回到默认值（`popular_by_day` / 今天）。
> ⚠️ **代理只有一个写入口**：`save-app-config`。它会把 `useProxy` / `proxyPort` 同步进 `yandere` / `pixiv` 子配置；
> 子配置的 save 会**忽略**代理字段 —— 这是为了避免"旧端口被写回"（历史上出现过）。

下载历史 `history.json`：记录已下载项，用于**去重**与失败重试。

---

## 十、已知问题与技术债

### 10.1 需要清理的死代码
| 位置 | 说明 |
|---|---|
| `src/main/index.js` 的 `derivePixivOriginalUrl()` | **已无任何调用**（旧版用缩略图推导原图，现改用 `/pages` 接口），可删 |
| `components/CategoryBar.jsx` | 未被引用（分区按钮已迁移到 `TypeTabs`） |
| `components/SearchBar.jsx` / `SettingsBar.jsx` | 未被引用；⚠️ 两个文件的**导出组件名互相颠倒**（`SearchBar.jsx` 导出的是 `SettingsBar`，反之亦然），删除前注意 |

### 10.2 限制与坑
| 问题 | 说明 |
|---|---|
| **pixiv 普通榜单没有 `aiType`** | `/ranking.php` 不返回该字段，所以"显示 AI"过滤在普通榜单下**不生效**（AI 榜本身仍是 AI 作品） |
| **R-18 榜无法查历史** | 带 `date` 请求 R-18 榜时，服务端返回的是今日榜单 |
| **关注流需登录** | 未登录时「我的关注」无数据；R-18 内容还需在 pixiv 账号设置里开启（设置页有跳转按钮） |
| **"关注按钮"改成了「打开作者主页」** | pixiv 关注接口实测需要更多签名 / 头信息，不稳定；改为调用系统浏览器打开作者页（`open-external`）。`pixiv-follow-user` 通道**仍保留**，只是 UI 未使用 |
| **「清理并删除」仍用原生 `window.confirm`** | 唯一保留的原生弹窗（样式不可控）；其余提示统一走 `toast()` |
| **`App.jsx` 已约 1000 行** | 建议后续按来源拆 hooks（如 `useWallhaven` / `useYandere` / `usePixiv`） |
| **`resources/icon.ico` 缺失** | `package.json` 的 `build.win.icon` 引用它，但**根目录没有 `resources/`**（只有空的 `src/resources/`）。实测 `npm run dist` **仍能成功**（回退成 Electron 默认图标），只是图标不是自己的 |
| ~~`.git` 体积异常~~ **已解决（437 MB → 161 KB）** | 历史误提交了 `release/`（`win-unpacked/*.exe` 165 MB + 安装包 72 MB），`e98ec7b` 只移出了工作区、对象仍留在历史里。**已重建仓库**：分支 `main`，原提交历史留档于 `git-history-backup.txt`（其中 400+ 条为 Cline 自动 checkpoint，实际业务提交仅 9 条）。⚠️ 仍需 `git push --force -u origin main` 覆盖远程旧历史 |
| ~~`dist/` 被 git 跟踪~~ **已解决** | 重建后的 `.gitignore`（`dist/`、`release/`、`out/`、`_tmp_*`）已生效，`git status` 干净，构建产物不再产生噪音 diff |
| **vite 代理端口取自用户配置** | `electron.vite.config.js` 在 dev 启动时读用户 `config.json`；用户改端口后需**重启 dev** 才生效 |
| ~~`electron-updater` 未配置~~ **已解决（v1.3.1）** | 已接入 GitHub Releases：`build.publish` + `npm run release` + 设置页「软件更新」区块 + `src/main/updater.js`。⚠️ 老用户装的是没有更新能力的旧版，必须**手动装一次 1.3.1**，从 1.3.1 起才能自动更新到 1.3.2+ |

### 10.3 易踩的坑（改代码必看）
1. **新增 pixiv 榜单** → 必须同时改 `TypeTabs.jsx` 的 `PIXIV_TABS` **和** `main/index.js` 的 `RANKING_MODES`
2. **改代理逻辑** → 只有一个入口 `save-app-config`，别在子配置里写端口
3. **改全屏大图** → 不要给 `<img>` 加 `${index}_${src}` 形式的 key（会黑闪）
4. **改图片加载** → pixiv 图片依赖 `webRequest` 注入 Referer，别删 `setupPixivImageNetworking`
5. **改缓存** → 登录 / cookie 变化时记得 `clearAllCaches()`
6. **发版** → 必须先改 `package.json` 的 `version`；`electron-updater` 必须留在 `electron.vite.config.js` 的 `external` 里
7. **本机环境** → `ELECTRON_RUN_AS_NODE` 必须不存在（连 `npm run dev` 都会被它搞崩，表现为 `app` 为 undefined）
8. **发布** → `electron-builder` **不读 `.env`**（必须由 `scripts/release.cjs` 注入 `GH_TOKEN`）；`releaseType` 默认 `draft`，草稿用户端看不到

---

## 十一、开发 / 构建 / 打包

```bash
npm install          # 安装依赖
npm run dev          # 开发（electron-vite dev，HMR；自动开 DevTools）
npm run build        # 构建到 dist/（三进程）
npm run preview      # 预览构建结果（等同 npm run start）
npm run dist         # 打包 Windows 安装包（仅本地，不上传；输出到 release/）
npm run release      # 打包并发布到 GitHub Release（需 GH_TOKEN，见 6.8 节）
```

**dev 与生产的差异**
| 项 | dev | 生产 |
|---|---|---|
| 渲染层加载 | `http://localhost:5173` | `dist/renderer/index.html` |
| Wallhaven API | `/api/v1`（vite server.proxy 转发，端口读用户配置） | `https://wallhaven.cc/api/v1` 直连 |
| DevTools | 自动打开 | 不打开 |

**打包配置要点**（`package.json` → `build`）
- `appId`：`com.wallhaven.downloader`；`productName`：`Wallhaven Downloader`
- `directories.output`：`release/`
- `files`：`dist/**/*`、`node_modules/**/*`
- `win.icon`：`resources/icon.ico`（⚠️ 该文件**当前缺失**：实测不影响打包，会回退成默认图标）
- `win.artifactName`：`Wallhaven-Downloader-Setup-${version}.${ext}` —— **必须显式指定**。否则安装包名含空格（`Wallhaven Downloader Setup 1.0.0.exe`），与 `latest.yml` 里的 url 对不上，自动更新会 404（详见第十节）
- `nsis`：非一键安装、允许改安装目录、创建桌面快捷方式
- ⚠️ **未配置 `extraResources` 与 `publish`**（自动更新链路未接通）

---

## 十二、交接必读注意事项

1. **本机不得存在 `ELECTRON_RUN_AS_NODE` 环境变量**
   - 若存在，Electron 会以纯 Node 模式启动（`app` / `BrowserWindow` 全为 `undefined`），表现为**启动即崩**
   - 检查：`node -e "console.log(process.env.ELECTRON_RUN_AS_NODE)"` → 应输出 `undefined`
   - 处理：从系统环境变量中删除（删除后需重开终端）

2. **代理是硬前提**：三站都需外网。若图片全部裂开，先查「设置 → 系统设置」的代理开关与端口。

3. **pixiv 图片地址必须带 Referer**：`i.pximg.net` 有防盗链，靠主进程 `webRequest` 注入，**别在渲染层自己 fetch**。

4. **改源码后注意重启**：`npm run dev` 下主进程改动会自动重启；但 `hardwareAcceleration` 这类**只在启动时读取**的项，必须在设置里保存后**手动重启应用**才生效。

5. **用户数据位置**：`%APPDATA%/wallhaven-downloader/`（`config.json`、`history.json`、图片缓存目录）。排查问题时先备份再删除。
   - ⚠️ 若同时存在 `%APPDATA%/wallhaven-electron/`，那是**更早版本**（当时 `package.json` 的 name 不同）留下的旧目录，**当前代码不会读它**

6. **UI 提示统一用 `toast()`**：不要再用 `alert`（已全面替换；`window.confirm` 仅"清理并删除"一处保留）。

7. **改完务必 `npm run build` 验证**：语法 / 导入错误能立刻暴露。

---

## 十三、快速定位索引

| 我想改… | 去哪里 |
|---|---|
| 顶部数据源按钮 / 整体布局 | `App.jsx` 顶部 render + `App.css`（`.top-bar` / `.type-tabs`）+ `TypeTabs.jsx` |
| 新增 / 修改榜单类型 | `TypeTabs.jsx`（`PIXIV_TABS`）+ `main/index.js`（`RANKING_MODES`） |
| pixiv 接口 / URL 规则 | `main/pixivApi.js` |
| pixiv 列表解析（含 aiType） | `main/index.js` → `fetch-pixiv-posts` |
| pixiv 图片能否显示（Referer / 代理） | `main/index.js` → `setupPixivImageNetworking()` |
| 下载并发 / 间隔 / 命名 | 三个 `*DownloadManager.js` |
| 下载进度推送频率 | `main/index.js` → `createProgressSender()` |
| 设置页各分区 | `SettingsPanel.jsx`（按 `activeTab` 分支） |
| 图片网格 / 角标 / 无限滚动 | `ImageGrid.jsx` |
| 全屏大图 / 画质 / 预加载 | `PixivIllustDetail.jsx` + `App.css`（`.illust-fullview-*`） |
| 左栏（各站） | `Sidebar.jsx` / `YandeRePanel.jsx` / `PixivPanel.jsx` |
| 下载面板 | `DownloadPanel.jsx` |
| 配置读写 / 持久化 | `main/store.js` |
| 缓存策略 | `main/cache.js` |
| 代理 agent | `main/proxyHelper.js` |
| 自动更新 | `main/updater.js` + 设置页「系统设置 → 软件更新」（发版流程见 6.8 节） |

---

## 十四、最近一轮变更摘要

> 供了解"为什么现在是这个样子"；更早的历史见 `git log`。

### 新增
- **pixiv 数据源（完整功能）**：账号登录、12 个浏览类型、历史榜单、搜索、作品详情页（多页 / 全屏原图 / 直链 / 选页下载）、R-18 与 AI 过滤、AI 与多页角标
- **顶部"分区 / 浏览类型"标签栏**（`TypeTabs.jsx`），标签可自定义显隐
- **设置页分区化**（系统设置 / Wallhaven / yande.re / pixiv 四个标签）
- **三站独立下载目录**
- `cache.js`（TTL+LRU）、`proxyHelper.js`（agent 复用）、`Toast.jsx`（替代 alert）
- 硬件加速开关、默认图片来源、清空缓存 / 清理并删除、无限滚动

### 自动更新（v1.3.1 新增）
- 新增 `src/main/updater.js`：检查 / 下载 / 安装 + 状态推送；打包环境下启动 8 秒后静默检查
- 设置页「系统设置 → 软件更新」：当前版本、检查更新、下载进度条、重启并安装；发现新版时 App 弹 Toast
- `package.json`：`version` 1.0.0 → **1.3.1**、新增 `build.publish`（GitHub）、`win.artifactName`、`npm run release`
- 顺带修掉两个会致命的问题：
  1. 安装包名与 `latest.yml` 不一致 → 自动更新 404（统一 `artifactName` 命名）
  2. `electron-updater` 被打包进 bundle 后**启动即崩** → 改为 external 依赖
- 让更新请求走用户代理（electron-updater 使用独立 session，默认不继承 defaultSession 的代理）

### 架构级替换
- pixiv 数据获取：HTML 正则解析 → **官方 JSON 接口**
- pixiv 图片加载：`pixiv-img://` 自定义协议 → **渲染层直连 `i.pximg.net`**（主进程注入 Referer）
- 代理：分散设置 → **会话级 `setProxy` + 请求级 agent 复用**，统一由 `save-app-config` 写入

### 修复 / 优化
- 下载进度 IPC 洪泛 → 150ms 合并推送
- history 每次写盘 → 800ms 防抖 + 退出时 flush
- 每张图重建代理 agent → 按端口缓存复用（走 VPN 时提速明显）
- 全屏"中等图 → 原图"切换**黑闪** → 双层叠加 + 原位淡入（不重建元素）
- 清理死代码：`pixiv-img://` 协议、`ImagePreview` 的 pixiv 分支、`rankingModes` / `hotTypes` / `handleCategoryClick` 等；`alert` 全部替换为 `toast`
- **设置页在非全屏窗口下看不到底部设置** → `.settings-content` 补 `flex:1 / min-height:0 / overflow-y:auto`（内容改为内部滚动），分区标签栏 `position:sticky` 吸顶，矮窗口（`max-height:620px`）自动压缩间距，并支持 `Esc` 关闭设置页

### 已知遗留（详见第十节）
- `derivePixivOriginalUrl()` 死代码未删
- 3 个未使用的组件文件（其中两个文件名与导出组件名颠倒）
- `App.jsx` 体积偏大、`resources/icon.ico` 缺失、`electron-updater` 未配置
- 普通榜单无 `aiType`，"显示 AI"过滤对普通榜单无效

---

**文档结束。** 如发现本文档与代码不一致，**以代码为准**，并顺手更新本文档对应段落。
